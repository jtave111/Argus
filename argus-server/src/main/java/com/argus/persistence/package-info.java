/**
 * Acesso a dados via JOOQ (PostgreSQL). Repositórios que executam o SQL de
 * {@code /sql/queries.sql}. O schema é versionado pelo Flyway em
 * {@code resources/db/migration}.
 */
package com.argus.persistence;
